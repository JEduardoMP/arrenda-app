import styled from "styled-components";

export const SubmitSt = styled.input`
  border: none;
  background-color: var(--secondaryBG);
  color: white;
  font-weight: 500;
  padding: 0.3rem 0;
  border-radius: 0.3rem;
`